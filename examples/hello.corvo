# hello.corvo - Hello World example

@welcome = procedure(@who, @line) {
    @line = string.concat("Welcome to ", @who, "!")
}

sys.echo("Hello, World!")

@name = "Corvo"
@msg = ""
@welcome.call(@name, @msg)
sys.echo(@msg)
